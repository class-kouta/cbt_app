# Step 4〜9 修正箇所リスト

> 本ファイルは `マルチテナント化対応計画.md` の **Step 4（認証ミドルウェア）〜 Step 9（コントローラ修正）** を機能ごとに分解した修正タスクリスト。
>
> 各項目は原則、認証（middleware）→ モデル（resolveRouteBinding）→ リポジトリインタフェース → リポジトリ実装 → ユースケース → コントローラ の順に並ぶ。

---

# 共通（横断的修正）

## Step 4-1: `routes/api.php`（認証ミドルウェア）

- [x] `routes/api.php` の `auth/register` / `auth/login` / `auth/email/resend` / `auth/email/verify` 以外の全 API ルートを `Route::middleware('auth:sanctum')->group(...)` 配下に移動

## Step 4-2: `routes/web.php`（認証ミドルウェア）

- [x] `routes/web.php` の `/` / `/login` / `/register` / `/verify-email` / `/siteAdmPanel63/...` 以外の全画面ルートを `Route::middleware('auth')->group(...)` 配下に移動

## Step 4-3: `bootstrap/app.php`（未認証リダイレクト）

- [x] `bootstrap/app.php` の `withMiddleware()` に `$middleware->redirectGuestsTo(fn () => route('login'))` を追加

## Step 5: 全モデルへの `member_id` 対応（15 モデル共通）

各モデルに以下を実施:

- `$fillable` に `'member_id'` を追加
- `member()` リレーション（`belongsTo(\App\Models\Member::class)`）を追加
- `resolveRouteBinding($value, $field = null)` をオーバーライドし `where('member_id', Auth::id())` で絞り込み

### マルチレコード型（10 モデル）

- [x] `app/Infrastructure/Database/Models/Coping.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/Column.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/WritingDisclosure.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/ProblemSolving.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/SimpleNotepad.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/StressorAndResponse.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/SupportNetwork.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/SchemaModeMonitoring.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/DialogueWork.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正（DialogueWork / ModeDialogueWork の両方で共有）
- [x] `app/Infrastructure/Database/Models/Chronology.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正

### シングルレコード型（5 モデル）

- [x] `app/Infrastructure/Database/Models/EarlyMaladaptiveSchema.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/SafePlace.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/ModeMap.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/HappySchemaActionPlan.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正
- [x] `app/Infrastructure/Database/Models/HealthyAdultModeImage.php` の `$fillable` / `member()` / `resolveRouteBinding()` を修正

---

# コーピングリスト

## 一覧取得（GET `/api/copings`）

- [ ] `app/Http/Controllers/CopingController.php` の `index()` を修正（`Coping::with('copingTags')` を `auth()->id()` の memberId で絞り込み。理想的にはユースケース化）
- [ ] `app/Domain/Repository/CopingRepositoryInterface.php` に `findAllForMember(int $memberId): array` を追加
- [ ] `app/Infrastructure/Repository/EloquentCopingRepository.php` に `findAllForMember()` を実装（`where('member_id', $memberId)->orderByDesc('point')->orderByDesc('created_at')`）

## 新規登録（POST `/api/copings`）

- [ ] `app/Http/Controllers/CopingController.php` の `store()` を修正（UseCase 経由で memberId を渡す）
- [ ] `app/Application/UseCase/Coping/CreateCopingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/CopingRepositoryInterface.php` の `save()` を `saveForMember(Coping $coping, int $memberId): Coping` に変更
- [ ] `app/Infrastructure/Repository/EloquentCopingRepository.php` の `save()` を `saveForMember()` に変更（新規時 `$model->member_id = $memberId`、更新時 `where('member_id', $memberId)` で絞り込み）

## 更新（PUT `/api/copings/{coping}`）

- [ ] `app/Http/Controllers/CopingController.php` の `update()` を修正（UseCase 経由で memberId を渡す）
- [ ] `app/Application/UseCase/Coping/UpdateCopingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/CopingRepositoryInterface.php` の `findById()` を `findByIdForMember(int $id, int $memberId): ?Coping` に変更
- [ ] `app/Infrastructure/Repository/EloquentCopingRepository.php` の `findById()` を `findByIdForMember()` に変更（`where('member_id', $memberId)` 追加）

## 削除（DELETE `/api/copings/{coping}`）

- [ ] `app/Http/Controllers/CopingController.php` の `destroy()` を修正（UseCase 経由で memberId を渡す）
- [ ] `app/Application/UseCase/Coping/DeleteCopingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/CopingRepositoryInterface.php` の `delete()` を `deleteForMember(int $id, int $memberId): void` に変更
- [ ] `app/Infrastructure/Repository/EloquentCopingRepository.php` の `delete()` を `deleteForMember()` に変更（`where('member_id', $memberId)` 追加）

---

# コラム法

## 一覧取得（GET `/api/columns`）

- [x] `app/Http/Controllers/ColumnController.php` の `index()` を修正（SearchUseCase に memberId を委譲）
- [x] `app/Application/UseCase/Column/SearchColumnUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchForMember()` 呼び出し）
- [x] `app/Domain/Repository/ColumnRepositoryInterface.php` の `search()` を `searchForMember(SearchCriteriaData $criteria, array $searchableColumns, int $memberId): array` に変更
- [x] `app/Infrastructure/Repository/EloquentColumnRepository.php` の `search()` を `searchForMember()` に変更（`where('member_id', $memberId)` を追加）

## 詳細取得（GET `/api/columns/{column}`）

- [x] `app/Http/Controllers/ColumnController.php` の `show()` は Route Model Binding（`Column $column`）で自動絞り込みされるが、`Column` モデルに `resolveRouteBinding()` が追加されていることを確認

## 適応的思考一覧取得（GET `/api/columns/adaptive-thoughts`）

- [x] `app/Http/Controllers/ColumnController.php` の `adaptiveThoughts()` の `Column::whereNotNull('adaptive_thought')` クエリに `where('member_id', Auth::id())` を追加（または専用 UseCase を新設）

## 新規登録（POST `/api/columns`）

- [x] `app/Http/Controllers/ColumnController.php` の `store()` を修正（UseCase 経由で memberId を渡す）
- [x] `app/Application/UseCase/Column/CreateColumnUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveWithTagsForMember()` 呼び出し）
- [x] `app/Domain/Repository/ColumnRepositoryInterface.php` の `save()` / `saveWithTags()` を `saveForMember()` / `saveWithTagsForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentColumnRepository.php` の `save()` / `saveWithTags()` を `saveForMember()` / `saveWithTagsForMember()` に変更（新規時 `$model->member_id = $memberId`、更新時 `where('member_id', $memberId)` で絞り込み）

## 更新（PUT `/api/columns/{column}`）

- [x] `app/Http/Controllers/ColumnController.php` の `update()` を修正（UseCase 経由で memberId を渡す）
- [x] `app/Application/UseCase/Column/UpdateColumnUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveWithTagsForMember()` 呼び出し）
- [x] `app/Domain/Repository/ColumnRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentColumnRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/columns/{column}`）

- [x] `app/Http/Controllers/ColumnController.php` の `destroy()` を修正（UseCase 経由で memberId を渡す）
- [x] `app/Application/UseCase/Column/DeleteColumnUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [x] `app/Domain/Repository/ColumnRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentColumnRepository.php` の `delete()` を `deleteForMember()` に変更

## CSV 出力（GET `/api/columns/export/csv`）

- [x] `app/Http/Controllers/ColumnController.php` の `exportCsv()` を修正（UseCase 経由で memberId を渡す）
- [x] `app/Application/UseCase/Column/ExportColumnCsvUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchAllForMember()` 呼び出し）
- [x] `app/Domain/Repository/ColumnRepositoryInterface.php` の `searchAll()` を `searchAllForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentColumnRepository.php` の `searchAll()` を `searchAllForMember()` に変更（`where('member_id', $memberId)` を追加）

---

# 筆記開示

## 一覧取得（GET `/api/writing-disclosures`）

- [x] `app/Http/Controllers/WritingDisclosureController.php` の `index()` を修正（UseCase 化または直クエリに `where('member_id', Auth::id())` を追加）
- [x] `app/Domain/Repository/WritingDisclosureRepositoryInterface.php` に `findAllForMember(int $memberId): array` を追加
- [x] `app/Infrastructure/Repository/EloquentWritingDisclosureRepository.php` に `findAllForMember()` を実装

## 新規登録（POST `/api/writing-disclosures`）

- [x] `app/Http/Controllers/WritingDisclosureController.php` の `store()` を修正
- [x] `app/Application/UseCase/WritingDisclosure/CreateWritingDisclosureUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [x] `app/Domain/Repository/WritingDisclosureRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentWritingDisclosureRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/writing-disclosures/{writingDisclosure}`）

- [x] `app/Http/Controllers/WritingDisclosureController.php` の `update()` を修正
- [x] `app/Application/UseCase/WritingDisclosure/UpdateWritingDisclosureUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [x] `app/Domain/Repository/WritingDisclosureRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentWritingDisclosureRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/writing-disclosures/{writingDisclosure}`）

- [x] `app/Http/Controllers/WritingDisclosureController.php` の `destroy()` を修正
- [x] `app/Application/UseCase/WritingDisclosure/DeleteWritingDisclosureUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [x] `app/Domain/Repository/WritingDisclosureRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [x] `app/Infrastructure/Repository/EloquentWritingDisclosureRepository.php` の `delete()` を `deleteForMember()` に変更

---

# 問題解決法

## 一覧取得（GET `/api/problem-solvings`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `index()` を修正
- [ ] `app/Application/UseCase/ProblemSolving/SearchProblemSolvingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `search()` を `searchForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `search()` を `searchForMember()` に変更（`where('member_id', $memberId)` を追加）

## 詳細取得（GET `/api/problem-solvings/{problemSolving}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `show()` は Route Model Binding で自動絞り込みされるが、`ProblemSolving` モデルに `resolveRouteBinding()` が追加されていることを確認

## 計画一覧取得（GET `/api/problem-solvings/plans`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `plans()` を修正
- [ ] `app/Application/UseCase/ProblemSolving/SearchPlanUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchPlansForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `searchPlans()` を `searchPlansForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `searchPlans()` を `searchPlansForMember()` に変更（`whereHas('problemSolving', fn ($q) => $q->where('member_id', $memberId))` を追加）

## 未完了振り返りチェック（GET `/api/problem-solvings/has-overdue-reflection`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `hasOverdueReflection()` のクエリに `whereHas('problemSolving', fn ($q) => $q->where('member_id', Auth::id()))` を追加

## CSV 出力（GET `/api/problem-solvings/export/csv`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `exportCsv()` を修正
- [ ] `app/Application/UseCase/ProblemSolving/ExportProblemSolvingCsvUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchAllForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `searchAll()` を `searchAllForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `searchAll()` を `searchAllForMember()` に変更

## 新規登録（POST `/api/problem-solvings`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `store()` を修正（タグ sync も UseCase に移すか、memberId 込みで処理）
- [ ] `app/Application/UseCase/ProblemSolving/CreateProblemSolvingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/problem-solvings/{problemSolving}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `update()` を修正
- [ ] `app/Application/UseCase/ProblemSolving/UpdateProblemSolvingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/problem-solvings/{problemSolving}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/ProblemSolving/DeleteProblemSolvingUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `delete()` を `deleteForMember()` に変更

## 解決策追加（POST `/api/problem-solvings/{problemSolving}/solutions`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `addSolution()` を修正（`$problemSolving` は Route Model Binding で絞り込み済み）
- [ ] `app/Application/UseCase/ProblemSolving/AddSolutionUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` でチェック後に `saveSolution()`）
- [ ] `app/Domain/Repository/ProblemSolvingRepositoryInterface.php` の `findById()` / `saveSolution()` を必要に応じて `ForMember` 版に変更
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の該当メソッドを修正

## 解決策更新（PUT `/api/problem-solvings/{problemSolving}/solutions/{solution}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `updateSolution()` に親所有者チェック（`$solution->problem_solving_id !== $problemSolving->id` の場合 `abort(404)`）を追加
- [ ] `app/Application/UseCase/ProblemSolving/UpdateSolutionUseCase.php` の `handle()` を修正（memberId と親の所有関係をチェック）
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `updateSolution()` で親 `problem_solvings.member_id` を経由した所有者チェックを追加

## 解決策削除（DELETE `/api/problem-solvings/{problemSolving}/solutions/{solution}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `deleteSolution()` に親所有者チェック（`$solution->problem_solving_id !== $problemSolving->id` の場合 `abort(404)`）を追加
- [ ] `app/Application/UseCase/ProblemSolving/DeleteSolutionUseCase.php` の `handle()` を修正
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `deleteSolution()` で親 `problem_solvings.member_id` 経由の所有者チェックを追加

## 計画追加（POST `/api/problem-solvings/{problemSolving}/plans`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `addPlan()` を修正（`$problemSolving` は Route Model Binding で絞り込み済み）
- [ ] `app/Application/UseCase/ProblemSolving/AddPlanUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` でチェック後に `savePlan()`）
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `savePlan()` の親所有者チェックを追加

## 計画更新（PUT `/api/problem-solvings/{problemSolving}/plans/{plan}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `updatePlan()` に親所有者チェック（`$plan->problem_solving_id !== $problemSolving->id` の場合 `abort(404)`）を追加
- [ ] `app/Application/UseCase/ProblemSolving/UpdatePlanUseCase.php` の `handle()` を修正
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `updatePlan()` / `findPlanById()` で親 `problem_solvings.member_id` 経由の所有者チェックを追加

## 計画削除（DELETE `/api/problem-solvings/{problemSolving}/plans/{plan}`）

- [ ] `app/Http/Controllers/ProblemSolvingController.php` の `deletePlan()` に親所有者チェック（`$plan->problem_solving_id !== $problemSolving->id` の場合 `abort(404)`）を追加
- [ ] `app/Application/UseCase/ProblemSolving/DeletePlanUseCase.php` の `handle()` を修正
- [ ] `app/Infrastructure/Repository/EloquentProblemSolvingRepository.php` の `deletePlan()` で親 `problem_solvings.member_id` 経由の所有者チェックを追加

---

# シンプルメモ帳

## 一覧取得（GET `/api/simple-notepads`）

- [ ] `app/Http/Controllers/SimpleNotepadController.php` の `index()` を修正（`SimpleNotepad::orderByDesc('created_at')` に `where('member_id', Auth::id())` を追加または UseCase 化）
- [ ] `app/Domain/Repository/SimpleNotepadRepositoryInterface.php` に `findAllForMember(int $memberId): array` を追加
- [ ] `app/Infrastructure/Repository/EloquentSimpleNotepadRepository.php` に `findAllForMember()` を実装

## 新規登録（POST `/api/simple-notepads`）

- [ ] `app/Http/Controllers/SimpleNotepadController.php` の `store()` を修正
- [ ] `app/Application/UseCase/SimpleNotepad/CreateSimpleNotepadUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SimpleNotepadRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSimpleNotepadRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/simple-notepads/{simpleNotepad}`）

- [ ] `app/Http/Controllers/SimpleNotepadController.php` の `update()` を修正
- [ ] `app/Application/UseCase/SimpleNotepad/UpdateSimpleNotepadUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SimpleNotepadRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSimpleNotepadRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/simple-notepads/{simpleNotepad}`）

- [ ] `app/Http/Controllers/SimpleNotepadController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/SimpleNotepad/DeleteSimpleNotepadUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SimpleNotepadRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSimpleNotepadRepository.php` の `delete()` を `deleteForMember()` に変更

---

# ストレッサーとストレス反応

## 一覧取得（GET `/api/stressor-and-responses`）

- [ ] `app/Http/Controllers/StressorAndResponseController.php` の `index()` を修正
- [ ] `app/Application/UseCase/StressorAndResponse/SearchStressorAndResponseUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchForMember()` 呼び出し）
- [ ] `app/Domain/Repository/StressorAndResponseRepositoryInterface.php` の `search()` を `searchForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentStressorAndResponseRepository.php` の `search()` を `searchForMember()` に変更（`where('member_id', $memberId)` を追加）

## 詳細取得（GET `/api/stressor-and-responses/{stressorAndResponse}`）

- [ ] `app/Http/Controllers/StressorAndResponseController.php` の `show()` は Route Model Binding で自動絞り込みされるが、`StressorAndResponse` モデルに `resolveRouteBinding()` が追加されていることを確認

## 新規登録（POST `/api/stressor-and-responses`）

- [ ] `app/Http/Controllers/StressorAndResponseController.php` の `store()` を修正
- [ ] `app/Application/UseCase/StressorAndResponse/CreateStressorAndResponseUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/StressorAndResponseRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentStressorAndResponseRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/stressor-and-responses/{stressorAndResponse}`）

- [ ] `app/Http/Controllers/StressorAndResponseController.php` の `update()` を修正
- [ ] `app/Application/UseCase/StressorAndResponse/UpdateStressorAndResponseUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/StressorAndResponseRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentStressorAndResponseRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/stressor-and-responses/{stressorAndResponse}`）

- [ ] `app/Http/Controllers/StressorAndResponseController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/StressorAndResponse/DeleteStressorAndResponseUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/StressorAndResponseRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentStressorAndResponseRepository.php` の `delete()` を `deleteForMember()` に変更

## CSV 出力（GET `/api/stressor-and-responses/export/csv`）

- [ ] `app/Http/Controllers/StressorAndResponseController.php` の `exportCsv()` を修正
- [ ] `app/Application/UseCase/StressorAndResponse/ExportStressorAndResponseCsvUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `searchAllForMember()` 呼び出し）
- [ ] `app/Domain/Repository/StressorAndResponseRepositoryInterface.php` の `searchAll()` を `searchAllForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentStressorAndResponseRepository.php` の `searchAll()` を `searchAllForMember()` に変更

---

# サポートネットワーク

## 一覧取得（GET `/api/support-networks`）

- [ ] `app/Http/Controllers/SupportNetworkController.php` の `index()` を修正（`SupportNetwork::orderByDesc('point')` に `where('member_id', Auth::id())` を追加または UseCase 化）
- [ ] `app/Domain/Repository/SupportNetworkRepositoryInterface.php` に `findAllForMember(int $memberId): array` を追加
- [ ] `app/Infrastructure/Repository/EloquentSupportNetworkRepository.php` に `findAllForMember()` を実装

## 新規登録（POST `/api/support-networks`）

- [ ] `app/Http/Controllers/SupportNetworkController.php` の `store()` を修正
- [ ] `app/Application/UseCase/SupportNetwork/CreateSupportNetworkUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SupportNetworkRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSupportNetworkRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/support-networks/{supportNetwork}`）

- [ ] `app/Http/Controllers/SupportNetworkController.php` の `update()` を修正
- [ ] `app/Application/UseCase/SupportNetwork/UpdateSupportNetworkUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SupportNetworkRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSupportNetworkRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/support-networks/{supportNetwork}`）

- [ ] `app/Http/Controllers/SupportNetworkController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/SupportNetwork/DeleteSupportNetworkUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SupportNetworkRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSupportNetworkRepository.php` の `delete()` を `deleteForMember()` に変更

---

# スキーマモードモニタリング（セルフモニタリング）

## 一覧取得（GET `/api/schema-mode-monitorings`）

- [ ] `app/Http/Controllers/SchemaModeMonitoringController.php` の `index()` を修正
- [ ] `app/Application/UseCase/SchemaModeMonitoring/ListSchemaModeMonitoringsUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findAllForMemberOrderByCreatedAtDesc()` 呼び出し）
- [ ] `app/Domain/Repository/SchemaModeMonitoringRepositoryInterface.php` の `findAllOrderByCreatedAtDesc()` を `findAllForMemberOrderByCreatedAtDesc(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentSchemaModeMonitoringRepository.php` の `findAllOrderByCreatedAtDesc()` を `findAllForMemberOrderByCreatedAtDesc()` に変更（`where('member_id', $memberId)` を追加）

## 詳細取得（GET `/api/schema-mode-monitorings/{schemaModeMonitoring}`）

- [ ] `app/Http/Controllers/SchemaModeMonitoringController.php` の `show()` は Route Model Binding で自動絞り込みされるが、`SchemaModeMonitoring` モデルに `resolveRouteBinding()` が追加されていることを確認

## 新規登録（POST `/api/schema-mode-monitorings`）

- [ ] `app/Http/Controllers/SchemaModeMonitoringController.php` の `store()` を修正
- [ ] `app/Application/UseCase/SchemaModeMonitoring/CreateSchemaModeMonitoringUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SchemaModeMonitoringRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSchemaModeMonitoringRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/schema-mode-monitorings/{schemaModeMonitoring}`）

- [ ] `app/Http/Controllers/SchemaModeMonitoringController.php` の `update()` を修正
- [ ] `app/Application/UseCase/SchemaModeMonitoring/UpdateSchemaModeMonitoringUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SchemaModeMonitoringRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSchemaModeMonitoringRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/schema-mode-monitorings/{schemaModeMonitoring}`）

- [ ] `app/Http/Controllers/SchemaModeMonitoringController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/SchemaModeMonitoring/DeleteSchemaModeMonitoringUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SchemaModeMonitoringRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSchemaModeMonitoringRepository.php` の `delete()` を `deleteForMember()` に変更

---

# 対話ワーク（ヘルシーサイドとスキーマサイドの対話）

> `dialogue_works` テーブルは `type` カラムで `healthy`（このセクション）と `mode`（後述のモードワーク対話）を切り分けている。モデルは共通だがコントローラ・ユースケースは別。

## 一覧取得（GET `/api/dialogue-works`）

- [ ] `app/Http/Controllers/DialogueWorkController.php` の `index()` を修正
- [ ] `app/Application/UseCase/DialogueWork/ListDialogueWorksUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findAllForMemberOrderByCreatedAtDesc()` 呼び出し）
- [ ] `app/Domain/Repository/DialogueWorkRepositoryInterface.php` の `findAllOrderByCreatedAtDesc()` を `findAllForMemberOrderByCreatedAtDesc(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentDialogueWorkRepository.php` の `findAllOrderByCreatedAtDesc()` を `findAllForMemberOrderByCreatedAtDesc()` に変更（`where('member_id', $memberId)->where('type', 'healthy')` を追加）

## 詳細取得（GET `/api/dialogue-works/{dialogueWork}`）

- [ ] `app/Http/Controllers/DialogueWorkController.php` の `show()` を修正（`findByIdForMember()` 呼び出し、または Route Model Binding で自動絞り込み）
- [ ] `app/Domain/Repository/DialogueWorkRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentDialogueWorkRepository.php` の `findById()` を `findByIdForMember()` に変更

## 新規登録（POST `/api/dialogue-works`）

- [ ] `app/Http/Controllers/DialogueWorkController.php` の `store()` を修正
- [ ] `app/Application/UseCase/DialogueWork/CreateDialogueWorkUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/DialogueWorkRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentDialogueWorkRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/dialogue-works/{dialogueWork}`）

- [ ] `app/Http/Controllers/DialogueWorkController.php` の `update()` を修正
- [ ] `app/Application/UseCase/DialogueWork/UpdateDialogueWorkUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）

## 削除（DELETE `/api/dialogue-works/{dialogueWork}`）

- [ ] `app/Http/Controllers/DialogueWorkController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/DialogueWork/DeleteDialogueWorkUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/DialogueWorkRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentDialogueWorkRepository.php` の `delete()` を `deleteForMember()` に変更

---

# モードワーク対話（ModeDialogueWork）

> 現状 UseCase 層を経由せず、コントローラから直接 `DialogueWork::where('type', 'mode')` で操作している。本対応では全メソッドに `member_id` 絞り込みを入れる。理想的には UseCase/Repository 経由に整理する。

## 一覧取得（GET `/api/mode-dialogue-works`）

- [ ] `app/Http/Controllers/ModeDialogueWorkController.php` の `index()` の `DialogueWork::where('type', 'mode')` に `where('member_id', Auth::id())` を追加

## 詳細取得（GET `/api/mode-dialogue-works/{modeDialogueWork}`）

- [ ] `app/Http/Controllers/ModeDialogueWorkController.php` の `show()` は Route Model Binding で自動絞り込みされるが、`DialogueWork` モデルに `resolveRouteBinding()` が追加されていることを確認（加えて `type === 'mode'` チェック維持）

## 新規登録（POST `/api/mode-dialogue-works`）

- [ ] `app/Http/Controllers/ModeDialogueWorkController.php` の `store()` の `DialogueWork::create([...])` に `'member_id' => Auth::id()` を追加

## 更新（PUT `/api/mode-dialogue-works/{modeDialogueWork}`）

- [ ] `app/Http/Controllers/ModeDialogueWorkController.php` の `update()` は Route Model Binding で自動絞り込みされるが、`type === 'mode'` チェック追加の検討（`show()` / `destroy()` と同様）

## 削除（DELETE `/api/mode-dialogue-works/{modeDialogueWork}`）

- [ ] `app/Http/Controllers/ModeDialogueWorkController.php` の `destroy()` は Route Model Binding で自動絞り込みされるが、既存の `type === 'mode'` チェック維持

---

# 年表

## 一覧取得（GET `/api/chronologies`）

- [ ] `app/Http/Controllers/ChronologyController.php` の `index()` を修正（`Chronology::orderByDesc('created_at')` に `where('member_id', Auth::id())` を追加または UseCase 化）
- [ ] `app/Domain/Repository/ChronologyRepositoryInterface.php` に `findAllForMember(int $memberId): array` を追加
- [ ] `app/Infrastructure/Repository/EloquentChronologyRepository.php` に `findAllForMember()` を実装

## 詳細取得（GET `/api/chronologies/{chronology}`）

- [ ] `app/Http/Controllers/ChronologyController.php` の `show()` は Route Model Binding で自動絞り込みされるが、`Chronology` モデルに `resolveRouteBinding()` が追加されていることを確認

## CSV 出力（GET `/api/chronologies/export/csv`）

- [ ] `app/Http/Controllers/ChronologyController.php` の `exportCsv()` を修正
- [ ] `app/Application/UseCase/Chronology/ExportChronologyCsvUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findAllForMember()` 呼び出し）

## 新規登録（POST `/api/chronologies`）

- [ ] `app/Http/Controllers/ChronologyController.php` の `store()` を修正
- [ ] `app/Application/UseCase/Chronology/CreateChronologyUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ChronologyRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentChronologyRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/chronologies/{chronology}`）

- [ ] `app/Http/Controllers/ChronologyController.php` の `update()` を修正
- [ ] `app/Application/UseCase/Chronology/UpdateChronologyUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ChronologyRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentChronologyRepository.php` の `findById()` を `findByIdForMember()` に変更

## 削除（DELETE `/api/chronologies/{chronology}`）

- [ ] `app/Http/Controllers/ChronologyController.php` の `destroy()` を修正
- [ ] `app/Application/UseCase/Chronology/DeleteChronologyUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `deleteForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ChronologyRepositoryInterface.php` の `delete()` を `deleteForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentChronologyRepository.php` の `delete()` を `deleteForMember()` に変更

---

# 早期不適応スキーマ（シングルレコード）

## 取得（GET `/api/early-maladaptive-schemas`）

- [ ] `app/Http/Controllers/EarlyMaladaptiveSchemaController.php` の `show()` の `EarlyMaladaptiveSchema::first()` を、`Auth::id()` の memberId で絞り込むように修正（リポジトリ経由の `findFirstForMember()` 呼び出しが望ましい）
- [ ] `app/Domain/Repository/EarlyMaladaptiveSchemaRepositoryInterface.php` の `findFirst()` を `findFirstForMember(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentEarlyMaladaptiveSchemaRepository.php` の `findFirst()` を `findFirstForMember()` に変更（`where('member_id', $memberId)->first()`）

## 新規登録（POST `/api/early-maladaptive-schemas`）

- [ ] `app/Http/Controllers/EarlyMaladaptiveSchemaController.php` の `store()` を修正
- [ ] `app/Application/UseCase/EarlyMaladaptiveSchema/CreateEarlyMaladaptiveSchemaUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/EarlyMaladaptiveSchemaRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentEarlyMaladaptiveSchemaRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/early-maladaptive-schemas/{earlyMaladaptiveSchema}`）

- [ ] `app/Http/Controllers/EarlyMaladaptiveSchemaController.php` の `update()` を修正
- [ ] `app/Application/UseCase/EarlyMaladaptiveSchema/UpdateEarlyMaladaptiveSchemaUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/EarlyMaladaptiveSchemaRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentEarlyMaladaptiveSchemaRepository.php` の `findById()` を `findByIdForMember()` に変更

---

# 安全な場所（シングルレコード）

## 取得（GET `/api/safe-places`）

- [ ] `app/Http/Controllers/SafePlaceController.php` の `show()` の `$repository->findFirst()` を `findFirstForMember(Auth::id())` に変更
- [ ] `app/Domain/Repository/SafePlaceRepositoryInterface.php` の `findFirst()` を `findFirstForMember(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentSafePlaceRepository.php` の `findFirst()` を `findFirstForMember()` に変更（`where('member_id', $memberId)->first()`）

## 新規登録（POST `/api/safe-places`）

- [ ] `app/Http/Controllers/SafePlaceController.php` の `store()` を修正
- [ ] `app/Application/UseCase/SafePlace/CreateSafePlaceUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SafePlaceRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSafePlaceRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/safe-places/{id}`）

- [ ] `app/Http/Controllers/SafePlaceController.php` の `update()` を修正（`int $id` バインドのため、UseCase 内で `member_id` 所有者チェックを実施）
- [ ] `app/Application/UseCase/SafePlace/UpdateSafePlaceUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/SafePlaceRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentSafePlaceRepository.php` の `findById()` を `findByIdForMember()` に変更

---

# モードマップ（シングルレコード）

## 取得（GET `/api/mode-maps`）

- [ ] `app/Http/Controllers/ModeMapController.php` の `show()` の `$repository->findFirst()` を `findFirstForMember(Auth::id())` に変更
- [ ] `app/Domain/Repository/ModeMapRepositoryInterface.php` の `findFirst()` を `findFirstForMember(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentModeMapRepository.php` の `findFirst()` を `findFirstForMember()` に変更（`where('member_id', $memberId)->first()`）

## CSV 出力（GET `/api/mode-maps/export/csv`）

- [ ] `app/Http/Controllers/ModeMapController.php` の `exportCsv()` を修正
- [ ] `app/Application/UseCase/ModeMap/ExportModeMapCsvUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findFirstForMember()` 呼び出し）

## 新規登録（POST `/api/mode-maps`）

- [ ] `app/Http/Controllers/ModeMapController.php` の `store()` を修正
- [ ] `app/Application/UseCase/ModeMap/CreateModeMapUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ModeMapRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentModeMapRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/mode-maps/{id}`）

- [ ] `app/Http/Controllers/ModeMapController.php` の `update()` を修正（`int $id` バインドのため、UseCase 内で `member_id` 所有者チェックを実施）
- [ ] `app/Application/UseCase/ModeMap/UpdateModeMapUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/ModeMapRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentModeMapRepository.php` の `findById()` を `findByIdForMember()` に変更

---

# ハッピースキーマと行動計画（シングルレコード）

> Step 7-a: `EloquentHappySchemaActionPlanRepository::findFirst()` を `latest()->first()` から `where('member_id', $memberId)->first()` に統一する（UNIQUE(member_id) 前提）。

## 取得（GET `/api/happy-schema-action-plans`）

- [ ] `app/Http/Controllers/HappySchemaActionPlanController.php` の `show()` の `$repository->findFirst()` を `findFirstForMember(Auth::id())` に変更
- [ ] `app/Domain/Repository/HappySchemaActionPlanRepositoryInterface.php` の `findFirst()` を `findFirstForMember(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentHappySchemaActionPlanRepository.php` の `findFirst()` を `findFirstForMember()` に変更（`latest()` 削除・`where('member_id', $memberId)->first()` に統一）

## CSV 出力（GET `/api/happy-schema-action-plans/export/csv`）

- [ ] `app/Http/Controllers/HappySchemaActionPlanController.php` の `exportCsv()` を修正
- [ ] `app/Application/UseCase/HappySchemaActionPlan/ExportHappySchemaActionPlanCsvUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findAllForMemberOrderedByLatest()` 呼び出し）
- [ ] `app/Domain/Repository/HappySchemaActionPlanRepositoryInterface.php` の `findAllOrderedByLatest()` を `findAllForMemberOrderedByLatest(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentHappySchemaActionPlanRepository.php` の `findAllOrderedByLatest()` を `findAllForMemberOrderedByLatest()` に変更

## 新規登録（POST `/api/happy-schema-action-plans`）

- [ ] `app/Http/Controllers/HappySchemaActionPlanController.php` の `store()` を修正
- [ ] `app/Application/UseCase/HappySchemaActionPlan/CreateHappySchemaActionPlanUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/HappySchemaActionPlanRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentHappySchemaActionPlanRepository.php` の `save()` を `saveForMember()` に変更（`updateOrCreate` の絞り込みキーに `member_id` を追加）

## 更新（PUT `/api/happy-schema-action-plans/{id}`）

- [ ] `app/Http/Controllers/HappySchemaActionPlanController.php` の `update()` を修正（`int $id` バインドのため、UseCase 内で `member_id` 所有者チェックを実施）
- [ ] `app/Application/UseCase/HappySchemaActionPlan/UpdateHappySchemaActionPlanUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/HappySchemaActionPlanRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentHappySchemaActionPlanRepository.php` の `findById()` を `findByIdForMember()` に変更

---

# ヘルシーな大人モードのイメージ（シングルレコード）

## 取得（GET `/api/healthy-adult-mode-images`）

- [ ] `app/Http/Controllers/HealthyAdultModeImageController.php` の `show()` の `$repository->findFirst()` を `findFirstForMember(Auth::id())` に変更
- [ ] `app/Domain/Repository/HealthyAdultModeImageRepositoryInterface.php` の `findFirst()` を `findFirstForMember(int $memberId)` に変更
- [ ] `app/Infrastructure/Repository/EloquentHealthyAdultModeImageRepository.php` の `findFirst()` を `findFirstForMember()` に変更（`where('member_id', $memberId)->first()`）

## 新規登録（POST `/api/healthy-adult-mode-images`）

- [ ] `app/Http/Controllers/HealthyAdultModeImageController.php` の `store()` を修正
- [ ] `app/Application/UseCase/HealthyAdultModeImage/CreateHealthyAdultModeImageUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/HealthyAdultModeImageRepositoryInterface.php` の `save()` を `saveForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentHealthyAdultModeImageRepository.php` の `save()` を `saveForMember()` に変更

## 更新（PUT `/api/healthy-adult-mode-images/{id}`）

- [ ] `app/Http/Controllers/HealthyAdultModeImageController.php` の `update()` を修正（`int $id` バインドのため、UseCase 内で `member_id` 所有者チェックを実施）
- [ ] `app/Application/UseCase/HealthyAdultModeImage/UpdateHealthyAdultModeImageUseCase.php` の `handle()` を修正（`Auth::id()` 取得 → `findByIdForMember()` / `saveForMember()` 呼び出し）
- [ ] `app/Domain/Repository/HealthyAdultModeImageRepositoryInterface.php` の `findById()` を `findByIdForMember()` に変更
- [ ] `app/Infrastructure/Repository/EloquentHealthyAdultModeImageRepository.php` の `findById()` を `findByIdForMember()` に変更

---

# 対象外（変更不要・スコープ外）

以下は `member_id` 非保有（共通マスター）または管理画面のためスコープ外。**変更しないこと**。

- [ ] `app/Http/Controllers/TagController.php` - 共通マスター（`tags`）
- [ ] `app/Http/Controllers/CopingTagController.php` - 共通マスター（`coping_tags`）
- [ ] `app/Http/Controllers/MindfulnessController.php` - 音声 URL 取得のみでデータ所有の概念なし
- [ ] `app/Http/Controllers/Auth/AuthController.php` - 認証系（ログイン・登録・検証など）
- [ ] 管理画面ルート（`/siteAdmPanel63/...`）および関連のコントローラ
